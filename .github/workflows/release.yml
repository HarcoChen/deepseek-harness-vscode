name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing release tag to publish"
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Resolve release metadata
        id: metadata
        shell: bash
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          NAME="$(node -p "require('./package.json').name")"
          TAG="${{ inputs.tag || github.ref_name }}"

          if [[ "${TAG}" != "v${VERSION}" ]]; then
            echo "Tag ${TAG} does not match package.json version v${VERSION}." >&2
            exit 1
          fi

          # A SemVer pre-release tag (0.6.0-beta.1) marks a build that is not
          # for the general channel. It still reaches Open VSX, but packaged
          # and published with `--pre-release`, so the registry flags it and
          # only clients whose user opted into pre-releases upgrade into it.
          # The GitHub release is marked pre-release as well. Note that the
          # VS Code Marketplace rejects SemVer pre-release suffixes outright,
          # so these versions never go there (that publish is manual anyway).
          PRERELEASE=false
          if [[ "${VERSION}" == *-* ]]; then
            PRERELEASE=true
          fi

          echo "version=${VERSION}" >> "${GITHUB_OUTPUT}"
          echo "vsix=${NAME}-${VERSION}.vsix" >> "${GITHUB_OUTPUT}"
          echo "prerelease=${PRERELEASE}" >> "${GITHUB_OUTPUT}"

      - name: Run checks
        run: npm test

      - name: Extract changelog entry
        env:
          VERSION: ${{ steps.metadata.outputs.version }}
        shell: bash
        run: |
          awk -v version="${VERSION}" '
            $0 == "## [" version "]" || index($0, "## [" version "] - ") == 1 { found=1; next }
            found && /^## \[/ { exit }
            found { print }
          ' CHANGELOG.md > release-notes.md

          if [[ ! -s release-notes.md ]]; then
            echo "CHANGELOG.md has no entry for ${VERSION}." >&2
            exit 1
          fi

      - name: Package extension
        shell: bash
        env:
          PRERELEASE: ${{ steps.metadata.outputs.prerelease }}
        # `--pre-release` writes the marker into the vsix manifest itself, so
        # the flag survives packaging and publishing as one decision.
        run: |
          if [[ "${PRERELEASE}" == "true" ]]; then
            npm run package -- --no-dependencies --pre-release
          else
            npm run package -- --no-dependencies
          fi

      - name: Publish to Open VSX
        shell: bash
        env:
          OVSX_PAT: ${{ secrets.OVSX_PAT }}
          VSIX: ${{ steps.metadata.outputs.vsix }}
          PRERELEASE: ${{ steps.metadata.outputs.prerelease }}
        run: |
          if [[ "${PRERELEASE}" == "true" ]]; then
            npx --yes ovsx publish "${VSIX}" -p "${OVSX_PAT}" --pre-release
          else
            npx --yes ovsx publish "${VSIX}" -p "${OVSX_PAT}"
          fi

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.metadata.outputs.vsix }}
          body_path: release-notes.md
          generate_release_notes: true
          fail_on_unmatched_files: true
          prerelease: ${{ steps.metadata.outputs.prerelease == 'true' }}
